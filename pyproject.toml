[project]
name = "resume-site"
version = "0.1.0"
description = "portfolio"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "webtypy>=0.1.7",
    "fastapi[standard]"
]

[dependency-groups]
dev = [
    "workers-py"
]

[assets]
directory = "./dist"
binding = "ASSETS"